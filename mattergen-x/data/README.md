# Data

Raw and processed datasets used for model training and validation.

## Purpose
- Host crystal structure files (e.g., CIF, POSCAR).
- Derived property tables (CSV, JSON).
- Data cleaning and preprocessing pipelines.
