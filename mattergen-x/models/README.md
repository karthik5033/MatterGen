# Models

Storage for saved model artifacts and architecture definitions.

## Purpose
- Store production-ready model weights (`.pt`, `.onnx`).
- Keep track of model versioning and metadata.
- Architecture-specific configuration files (e.g., GNN structures).
